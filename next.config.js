/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['picsum.photos', 'res.cloudinary.com', 'upload.wikimedia.org', 'cos.h-cdn.co'],
  },
};

module.exports = nextConfig;
